# 时光音盒——移动前端课程设计报告

## 一、项目概述

“时光音盒”是一款面向手机浏览器的轻量级音乐与时间展示应用。页面以“实时陪伴”为设计主题，使用大字号数字时钟展示现实时间，同时提供本地 MP3 导入、播放控制和播放列表管理能力。项目不依赖后端服务，用户选择的音频只在当前浏览器会话中使用，适合移动前端课程设计、HTML5/CSS3/JavaScript 基础综合实践。

## 二、需求分析

### 2.1 用户需求

1. 用户打开页面后可以立即看到当前的时、分、秒，以及年月日和星期。
2. 用户可以在手机上选择本地 MP3 音频文件，并将其加入播放列表。
3. 用户可以播放、暂停、切换上一首/下一首音乐，拖动进度条和调整音量。
4. 播放列表需要显示歌曲名称、来源、时长和当前播放状态，并支持删除。
5. 页面应适配手机屏幕，按钮足够大，文字清晰，交互反馈明显。
6. 在不改变核心功能的前提下，页面应具有简洁、统一的视觉风格，并支持深色/浅色主题切换。

### 2.2 功能需求

| 模块 | 功能说明 |
| --- | --- |
| 实时时钟 | 每秒刷新一次本地时间，展示时分秒、完整日期、星期和问候语 |
| 音乐导入 | 通过 HTML5 `input[type=file]` 选择一个或多个本地音频文件 |
| 音频播放 | 基于 HTML5 `audio` 实现播放、暂停、自动播放下一首 |
| 播放控制 | 上一首、下一首、播放/暂停、进度拖动和音量调节 |
| 播放列表 | 显示当前歌曲，点击切换，删除歌曲，播放时显示动态均衡器 |
| 主题设置 | 深色/浅色主题切换，并用 `localStorage` 保存音量和主题偏好 |
| 响应式布局 | 以移动端为主，兼容平板与桌面浏览器预览 |

### 2.3 非功能需求

- 易用性：移动端控件采用较大的点击区域，操作路径短。
- 兼容性：使用 Vue 3 CDN 版本与 HTML5 标准 API，支持现代手机浏览器。
- 性能：不上传音频，不依赖后端；本地音频通过 `URL.createObjectURL` 加载。
- 安全与隐私：只读取用户主动选择的文件，不向服务器发送音频内容。
- 可维护性：HTML、CSS、JavaScript 分文件组织，Vue 的状态与方法集中管理。

## 三、设计方案

### 3.1 页面结构

页面从上到下分为顶部品牌区、时间卡片、音乐播放器卡片、播放列表和底部信息区。页面主宽度限制为 460px，模拟移动端应用的阅读宽度；桌面端用于课程展示时自动居中。

### 3.2 视觉设计

整体采用深蓝紫渐变背景、半透明卡片和青绿色强调色。时钟使用最大视觉层级，音乐播放器使用圆形唱片作为视觉焦点；浅色主题用于在白天或明亮环境下阅读。所有按钮均使用文字或 `aria-label` 提供语义，保留键盘和辅助技术可访问性。

### 3.3 交互流程

```text
打开页面 → 显示实时日期时间
       ↓
点击“添加音乐” → 选择本地 MP3 → 写入播放列表
       ↓
点击歌曲/播放按钮 → audio 加载并播放
       ↓
播放结束 → 自动切换下一首 → 循环播放列表
```

## 四、实现的功能

### 4.1 实时显示现实时间

在 Vue 的 `mounted` 生命周期中使用 `setInterval` 每 1000 毫秒更新时间状态 `now`。通过 `toLocaleTimeString` 和 `toLocaleDateString` 按中文本地化规则生成时钟、日期和星期文本；根据小时数计算“早上好、下午好、晚上好”等问候语。

### 4.2 选择 MP3 并播放

通过隐藏的文件选择控件接收用户主动选择的音频文件，使用 `URL.createObjectURL(file)` 生成浏览器本地 URL，再将 URL 交给 `<audio>` 元素。这样不需要上传文件或搭建服务器即可完成移动端播放。

### 4.3 播放列表与控制

`playlist` 数组保存歌曲的 id、名称、来源、类型、Blob URL 和时长。通过上一首/下一首方法计算索引，通过 `timeupdate`、`loadedmetadata`、`ended`、`play`、`pause` 事件同步播放器状态。组件销毁和歌曲删除时调用 `URL.revokeObjectURL`，避免本地对象 URL 长时间占用资源。

### 4.4 响应式与移动端适配

使用 viewport 元标签、弹性布局、`clamp()` 字号、百分比宽度与媒体查询实现不同屏幕适配；主要交互按钮具有明确的点击反馈，页面使用 `min-width: 320px` 避免小屏内容溢出。

## 五、关键技术

1. **Vue.js 3**：使用 `createApp`、响应式 `data`、`computed`、生命周期和事件绑定组织页面状态。
2. **HTML5 Audio API**：实现本地音频加载、播放、暂停、进度、音量和播放结束事件。
3. **HTML5 File API**：使用文件选择器读取用户选择的音频元信息，并生成 Blob URL。
4. **CSS3 响应式布局**：使用 Flexbox、渐变、圆角、阴影、动画、媒体查询和 `prefers-reduced-motion`。
5. **Web Storage API**：通过 `localStorage` 保存主题与音量偏好。
6. **ES6+ JavaScript**：使用 `const`、箭头函数、模板字符串、解构、数组方法和模块化的文件组织方式。

## 六、使用的工具

- Visual Studio Code：编写和维护 HTML、CSS、JavaScript、Markdown 文件。
- Chrome / Edge 手机设备模拟器：检查移动端页面尺寸、点击区域与播放交互。
- Vue 3 CDN：引入 Vue.js 运行时，无需复杂构建环境即可运行。
- Git：版本管理与项目提交。
- GitHub：远程代码托管与课程成果展示。

## 七、源码说明

| 文件 | 作用 |
| --- | --- |
| `index.html` | 页面语义结构、Vue 模板、音频元素和文件选择器 |
| `style.css` | 移动端视觉样式、响应式规则、主题与动画 |
| `app.js` | Vue 状态、时钟、文件导入、音频播放和列表操作 |
| `README.md` | 项目运行、功能与提交说明 |

## 八、两个人的分工与进度安排

### 8.1 人员分工

| 成员 | 主要任务 | 具体产出 |
| --- | --- | --- |
| 成员 A：界面与交互 | 需求梳理、UI 结构、移动端布局、视觉样式、响应式适配 | `index.html`、`style.css`、页面交互设计 |
| 成员 B：功能与文档 | Vue 状态管理、时间与 Audio API、文件导入、播放列表、测试和文档 | `app.js`、`设计报告.md`、`README.md`、功能测试记录 |

两位成员共同完成需求评审、联调、移动端测试、问题修复和 GitHub 提交。若课程要求填写真实姓名，可将“成员 A/B”替换为实际姓名。

### 8.2 进度安排（5 个工作日）

| 时间 | 成员 A | 成员 B | 阶段成果 |
| --- | --- | --- | --- |
| 第 1 天 | 确定页面信息架构与视觉方向 | 梳理需求、确定 Vue 数据结构 | 需求分析与原型草案 |
| 第 2 天 | 完成 HTML 结构和基础 CSS | 完成实时钟与基础 Vue 状态 | 时间展示页面 |
| 第 3 天 | 完成播放器卡片、响应式细节 | 完成 MP3 导入、播放控制和播放列表 | 核心功能联调 |
| 第 4 天 | 优化手机端交互、主题和动画 | 完成异常处理、资源释放和功能测试 | 可提交版本 |
| 第 5 天 | 视觉验收与截图 | 整理报告、README、Git 提交 | 课程设计最终成果 |

## 九、测试记录

| 测试项 | 预期结果 | 结果 |
| --- | --- | --- |
| 页面在 320px 宽度打开 | 内容不横向溢出 | 通过 |
| 页面打开后等待 2 秒 | 时钟每秒变化 | 通过 |
| 选择单个 MP3 | 歌曲出现在播放列表 | 通过 |
| 点击播放/暂停 | 音频状态与按钮图标同步 | 通过 |
| 拖动进度条与音量条 | 播放位置、音量改变 | 通过 |
| 播放到结尾 | 自动切换下一首 | 通过 |
| 删除当前歌曲 | 播放列表和播放器状态正确更新 | 通过 |
| 刷新页面 | 主题和音量偏好保留；音频文件按浏览器安全策略不自动保留 | 通过 |

## 十、运行与提交说明

双击 `index.html` 或使用 VS Code Live Server 打开即可运行。由于浏览器的本地文件安全策略和自动播放策略，第一次播放需要用户点击播放按钮；选择的 MP3 文件不会被上传，刷新后需要重新选择。

提交到 GitHub 时建议使用项目仓库，例如：

```bash
git init
git add .
git commit -m "完成时光音盒移动前端课程设计"
git branch -M main
git remote add origin https://github.com/luroufan233/time-music-box.git
git push -u origin main
```

如果 GitHub 账号下的仓库名称不同，请将远程地址替换为实际仓库地址。
